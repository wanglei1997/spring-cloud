eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka
    fetch-registr: true
    register-with-eureka: true
  instance:
    hostname: localhost
    prefer-ip-address: true
spring:
  application:
    name: eureka-client
server:
  port: 9002